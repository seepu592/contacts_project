{{ form_start(form) }}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-12">
                <div class="form-group">
                    {{ form_widget(form) }}
                </div>
                <button class="btn btn-primary w-100">{{ button_label|default('Save') }}</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}
